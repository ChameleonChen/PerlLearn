#!/usr/bin/perl

require "tlv.pl";

#base info
my $base_name = "name";
my $base_priority = "priority";

my $file_total_Length = 0;
my $item_total_Length = 0;

#创建应用描述文件
sub write_app_item{
  my ($name_en, $priority) = @_;
}

#创建应用描述文件
sub create_app_des{
  my ($des_file, $name_en, $priority) = @_;
}

1;
